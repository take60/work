# scala

## 事前準備
```Bash
brew install scala
brew install sbt
```

## プロジェクト作成
```Bash:make_project
sh makeprojectsh ./
```

プロジェクト名とVersionを入れれば自動的にsbtのディレクトリが出来上がる。
サンプルのScalaコードもあるため、```sbt run```すれば実行可能

